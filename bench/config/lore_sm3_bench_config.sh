ITERATIONS=${ITERATIONS:-10000}
WARMUP=${WARMUP:-1000}
BUILD_MODE=${BUILD_MODE:-generic}
CFLAGS=${CFLAGS:-"-O3 -DNDEBUG"}
CPU_PIN=${CPU_PIN:-""}
RUN_KAT=${RUN_KAT:-1}
RUN_BENCH=${RUN_BENCH:-1}
LORE_SM3_REPO="${LORE_SM3_REPO:-$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)}"
export LORE_SM3_REPO="${LORE_SM3_REPO:-$(pwd)}"
